{
  "name": "velocity",
  "version": "1.2.1",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "npm run build && electron .",
    "dev": "npm run build && cross-env NODE_ENV=development electron .",
    "dev:watch": "npm run build && concurrently \"npm run build:watch\" \"nodemon --exec \\\"cross-env NODE_ENV=development electron .\\\" --watch dist --ext js\"",
    "clean": "rimraf dist",
    "electron": "npm run build && electron .",
    "pack": "npm run build && electron-builder --dir",
    "dist": "npm run build && electron-builder",
    "dist:win": "npm run build && electron-builder --win",
    "release": "npm run build && electron-builder --publish=always"
  },
  "keywords": [
    "electron",
    "typescript",
    "desktop",
    "emulator",
    "launcher",
    "gaming",
    "retro"
  ],
  "author": "Patonero",
  "license": "MIT",
  "description": "A modern, secure emulator launcher for Windows",
  "devDependencies": {
    "@jest/globals": "^30.0.5",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.1.0",
    "chokidar": "^4.0.3",
    "concurrently": "^9.2.0",
    "cross-env": "^10.0.0",
    "electron": "^37.2.5",
    "electron-builder": "^25.1.8",
    "electron-reload": "^2.0.0-alpha.1",
    "jest": "^30.0.5",
    "jest-environment-jsdom": "^30.0.5",
    "nodemon": "^3.1.10",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2",
    "wait-on": "^8.0.4"
  },
  "build": {
    "appId": "com.velocity.launcher",
    "productName": "Velocity Launcher",
    "directories": {
      "output": "dist"
    },
    "icon": "assets/icon.png",
    "files": [
      "dist/**/*",
      "renderer/**/*",
      "node_modules/**/*",
      "package.json",
      "!node_modules/**/*.md",
      "!node_modules/**/README*",
      "!node_modules/**/LICENSE*",
      "!node_modules/**/CHANGELOG*",
      "!node_modules/**/*.d.ts.map",
      "!node_modules/**/*.js.map",
      "!node_modules/**/test/**",
      "!node_modules/**/tests/**",
      "!node_modules/**/__tests__/**",
      "!node_modules/**/spec/**",
      "!node_modules/**/.git*",
      "!node_modules/**/docs/**",
      "!node_modules/**/example/**",
      "!node_modules/**/examples/**",
      "!node_modules/**/*.flow",
      "!dist/tests/**",
      "!dist/**/*.d.ts.map",
      "!dist/**/*.js.map"
    ],
    "extraResources": [
      {
        "from": "assets",
        "to": "assets",
        "filter": [
          "**/*.png",
          "**/*.ico",
          "!**/README*"
        ]
      }
    ],
    "compression": "maximum",
    "removePackageScripts": true,
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.png",
      "requestedExecutionLevel": "asInvoker",
      "artifactName": "${name}-${version}-${arch}.${ext}",
      "asar": true,
      "asarUnpack": [
        "node_modules/sharp/**/*",
        "node_modules/better-sqlite3/**/*"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Velocity Launcher"
    },
    "portable": {
      "artifactName": "${name}-${version}-portable.${ext}"
    },
    "publish": {
      "provider": "github",
      "owner": "Patonero",
      "repo": "velocity"
    }
  },
  "dependencies": {
    "electron-log": "^5.4.2",
    "electron-updater": "^6.6.2"
  }
}
