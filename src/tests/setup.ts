// Jest setup - runs once per test file after the test framework is installed.
// jest.config.js already handles mock clearing/resetting/restoring between
// tests (clearMocks/resetMocks/restoreMocks), so nothing else is needed here
// yet. Add global mocks or matchers here if future suites need them.
export {};
